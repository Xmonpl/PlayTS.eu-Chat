package cf.xmon.chat.utils;

public class AiUtil {
    public static void sendAIMessage(String s){
        if (s.toLowerCase().contains("jak to zmutowac") || s.toLowerCase().contains("jak to wyciszyc")){

        }
    }
}
